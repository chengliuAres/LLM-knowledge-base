# 技术设计文档 — 代码向量搜索能力评估

> 生成日期：2026-06-11 · 评估范围：`code_search.py` / `code_db.py` / `code_embedder.py` / `code_parser.py` / `query_translator.py`

---

## 一、整体架构

```
中文查询 → query_translator（翻译层）→ 英文关键词
                    ↓
    ┌───────────────┼───────────────┬───────────────┐
    │               │               │               │
 向量搜索    FTS5英文关键词    FTS5中文原文    符号名SQL搜索
(每词分别embed   (精确匹配)     (注释/字符串)   (symbol LIKE)
 递减权重RRF)                                    最精准路径
  权重 1.0        权重 1.0        权重 0.3       权重 1.5
    │               │               │               │
    └───────────────┴───────────────┴───────────────┘
                    ↓
            RRF 扁平融合排序 (k=60)
                    ↓
         匹配层级提权 (文件名1.5x > 类名1.4x > 方法1.2x)
         + 多词匹配叠加 (每多匹配一个关键词 +0.2x)
                    ↓
              文件级去重 → 返回结果
```

---

## 二、搜索功能模块 — 总体可以，但有前提条件

### ✅ 做得好的

| 策略 | 说明 |
|------|------|
| **四路混搜 + RRF 融合** | 向量路（每个关键词分别embed，递减权重 2.0/0.5，权重1.0）+ 英文FTS5（权重1.0）+ 中文FTS5（权重0.3）+ 符号名LIKE搜索（权重1.5，最精准路径）→ RRF 扁平融合去噪，比单一向量搜索召回率大幅提升 |
| **匹配层级提权** | 文件名匹配 1.5x，类名/interface 1.4x，方法名 1.2x，多词匹配叠加（每多匹配一个关键词 +0.2x），确保最相关的模块排第一 |
| **翻译层降级链** | 本地词典（贪心最长匹配 → jieba分词逐词匹配）→ 缓存（30天）→ MyMemory API → LLM → 原query回退，中文→英文代码词根的翻译质量是关键 |
| **向量阈值 0.5** | 砍掉余弦相似度<0.5的"最近邻但无关"结果，减少噪声 |
| **注释/中文剥离** | embedding 前清洗 C系注释和中文字面量，避免跨语言模型被中文query匹配到代码里硬编码的中文字符串 |
| **文件级去重** | 同一文件只保留得分最高的一条，避免一个类炸出十几个chunk霸榜 |

### ⚠️ 关键前提：翻译层质量决定搜索上限

整个向量搜索依赖 `bge-small-en-v1.5`（384维，英文模型），中文查询必须先翻译成英文关键词。如果翻译错误（比如 "读信" 译成 "letter" 而非 "read/mail/message"），向量路和英文FTS5路都会跑偏。符号名直接SQL搜索（`symbol_name LIKE '%kw%'`）是最可靠的路。

当前词典 `data/translation_dict.json` 覆盖了认证、邮件两大领域约 196 词条，未覆盖的词需要走 MyMemory/LLM，存在不稳定因素。

### 核心组件参数一览

| 组件 | 配置 | 说明 |
|------|------|------|
| Embedding 模型 | `BAAI/bge-small-en-v1.5` | 384维，英文，query侧加 bge 前缀，索引侧不加 |
| 文本截断 | 1100 字符 | 英文代码 ~3 chars/token，安全域内 |
| 向量阈值 | `MIN_VECTOR_SIMILARITY = 0.5` | score=(1+余弦相似度)/2，0.5=正交分界 |
| RRF 常量 | `RRF_K = 60` | 分母越大，排名差异越平滑 |
| 向量路权重 | 首词 2.0，后续词 0.5 | 递减策略，首词最重要 |
| FTS5 权重 | 英文 1.0，中文 0.3 | 中文路为弱信号补充 |
| 符号LIKE权重 | 1.5 | 最精准路径，兜底FTS5驼峰拆分盲区 |
| 提权系数 | 文件名1.5x / 类名1.4x / 方法1.2x | 可叠加，多词匹配每多一个+0.2x |
| 关键词数上限 | `MAX_KEYWORDS = 5` | 防止翻译层产出过多关键词导致串行检索延迟膨胀 |
| 驼峰拆词 | `_split_camel_case()` | GHMineViewController → [GH, Mine, View, Controller]，用于symbol LIKE搜索 |

---

## 三、调用栈追踪 — 能力有限，只做静态 1-hop

### trace_chain 实现机制

```
trace_code(symbol, depth=2, direction="both")
    │
    ├─ 1. 先混搜定位起始符号（hybrid search top 5）
    │
    └─ 2. 对每个匹配符号执行 trace_chain BFS
         │
         ├─ 查 callees: code_relations WHERE caller_chunk_id = ? (LIMIT 30)
         ├─ 查 callers: code_relations WHERE callee_name = ? (LIMIT 15)
         └─ 深度上限: 3 hops
```

### 数据来源

tree-sitter AST 静态解析时提取的调用关系，存入 SQLite `code_relations` 表：

| 字段 | 说明 |
|------|------|
| `caller_chunk_id` | 调用方 chunk ID |
| `caller_symbol_name` | 调用方符号名 |
| `caller_file_path` | 调用方文件路径 |
| `callee_name` | 被调用函数/方法名 |
| `callee_line` | 调用发生行号 |
| `repo_name` | 所属仓库 |

索引：`caller_chunk_id` / `(callee_name, repo_name)` / `repo_name`

唯一约束：`(caller_chunk_id, callee_name, callee_line, repo_name)` 防止重复写入

### ✅ 可以做到

- 静态调用链追踪（直接调用关系，如 `A.method1() → B.method2()`）
- 最多 3 跳 BFS 遍历
- 支持双向（谁调我 + 我调谁）
- 系统/框架方法通过黑名单过滤（`_SYSTEM_CALL_BLACKLIST` 约 90+ 条目），减少噪音
- 支持 ObjC / Swift / Java / Kotlin / Dart / C++ / Python / Ruby / JS / TS / Go / Rust 共 12 种语言的调用关系提取

### ❌ 做不到

| 场景 | 原因 |
|------|------|
| **ObjC 动态派发** | `[obj performSelector:@selector(xxx)]` — 选择器运行时才确定，tree-sitter 无法解析 |
| **block/closure 回调** | `completion:^{ ... }` — 匿名函数块没有符号名，不会被记录 |
| **delegate 模式** | `self.delegate.method()` — tree-sitter 只能提取到 `method` 被调用，但不知道实际是哪个类 |
| **通知/观察者** | `NSNotificationCenter.post()` — 运行时绑定，静态分析无法追踪 |
| **同名方法歧义** | `ViewController.init()` vs `Model.init()` — trace_chain 按符号名字符串匹配，不区分所属类，两个 `init` 会被当成同一个节点 |
| **虚函数/多态** | C++ `virtual`、Java `@Override` — 静态解析只能看到声明侧的调用，看不到运行时实际派发到哪个子类 |
| **间接调用** | 函数指针、函数作为参数传递 — tree-sitter 不追踪 |
| **跨文件引用未被调用** | 如果函数只被 import/reference 但 AST 中没找到 call 节点，就不会进入调用图 |
| **BFS 截断** | callee 方向 LIMIT 30，caller 方向 LIMIT 15，大文件/热点函数调用图会被截断 |

---

## 四、调用关系提取细节

### 语言支持（12 种）

| 语言 | 调用节点类型 | 方法名提取方式 |
|------|-------------|---------------|
| ObjC | `message_expression` | 解析 selector 各部分（identifier + `:` 对），拼接完整 selector |
| Swift | `call_expression`, `simple_identifier` | `simple_identifier` 或 `navigation_expression` 最后一段 |
| Java/Kotlin/Dart/C++ | `method_invocation` / `call_expression` / `function_invocation` | `identifier` 或 `template_function` 子节点 |
| Python | `call` | `attribute` → 取最后一段；或 `identifier` |
| JS/TS | `call_expression` | `member_expression` → `property_identifier`；或 `identifier` |
| Go/Rust | `call_expression` | `identifier` / `field_identifier` / `type_identifier` |
| Shell/YAML/JSON | — | 无结构化符号，不提取调用 |

### 系统调用黑名单（示例）

约 90+ 条目的 `_SYSTEM_CALL_BLACKLIST`，覆盖：

- **ObjC 生命周期**：`init`, `alloc`, `dealloc`, `viewDidLoad`, `viewWillAppear` ...
- **GCD/线程**：`dispatch_async`, `dispatch_sync` ...
- **Foundation 集合**：`count`, `length`, `addObject`, `removeObject` ...
- **Swift 高阶函数**：`map`, `filter`, `reduce`, `forEach` ...
- **Python 内置**：`len`, `range`, `isinstance` ...
- **JS/TS 内置**：`console.log`, `JSON.parse`, `setTimeout` ...
- **过于泛化的方法**：`get`, `set`, `update`, `delete`, `create`

---

## 五、核心结论

| 维度 | 评价 | 准确度 |
|------|------|--------|
| **找功能模块（类/文件级）** | 混搜 + 提权 + 去重，配合翻译层，中文query能找到对应类/文件 | ⭐⭐⭐⭐ 高 |
| **找具体方法** | 符号名精确搜索（`LIKE '%kw%'`）最可靠，向量路/英文FTS5做补充 | ⭐⭐⭐ 中高 |
| **静态直接调用链** | BFS 1-2跳内可以追踪明确的直接调用 | ⭐⭐⭐ 中 |
| **深度调用栈（3跳+）** | 会被 LIMIT 截断，且越深噪音越大 | ⭐⭐ 低 |
| **动态/间接调用栈** | 完全无法追踪 | ❌ 不支持 |

### 一句话总结

**找"哪个文件/类实现了XX功能"很靠谱。问"A方法最终调用了哪些底层函数，完整调用链是什么"，只能给出静态可见的直接调用关系，看不到运行时真实调用栈，Objective-C/Swift 的动态特性更是盲区。**
